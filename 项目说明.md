# 项目说明

本项目以直肠癌治疗前T2WI生境分析、A集内部验证及B集一次性设备域外部验证为当前主线。当前方法为 Primary Prognostic Analysis v2；科学主协议见《T2WI-HS-radiomics-analysis 后续探索性预后分析与双阶段冻结任务书.md》，Primary v2 合同见根目录 Primary v2 方案书和 `prognosis_analysis/primary/`。Formal W08 v1 已归档并被替代；FT 为 Primary v2 的历史开发来源。正式生境方法见《生境分析方案与工作流.md》，影像预处理技术细节见《组学分析方案.md》。

## 目录结构

```text
<project-root>\
├── feature_extract\                 原始影像、ROI及可复用的预处理/特征资产
├── habitat_analysis\                当前生境分析工作区
│   ├── configs\                     主参数和终点定义
│   ├── input\                       权威上游输入索引
│   ├── scripts\                     当前方案脚本
│   ├── output\                      分阶段结果目录
│   ├── README.md                    当前入口与执行边界
│   └── analysis_freeze.md           第一阶段技术冻结参数及状态
├── prognosis_analysis\              A集结局建模、最终模型冻结和B集验证工作区
├── manuscript\                      方法学证据与稿件阶段工作区
├── archive\                         历史协议及本地探索材料归档
│   ├── formal_nested_cv_v1\         Formal W08 v1 协议与审查归档
│   ├── ft_validation_v1\            FT 安全聚合证据归档
│   ├── project_status_history\      项目状态历史
│   └── protocol_history\            更早的任务书与审阅报告
├── T2WI-HS-radiomics-analysis 后续探索性预后分析与双阶段冻结任务书.md
│                                      科学主协议
├── T2WI-HS-radiomics-analysis Pre-W08 整改、协议补丁与后续 A-only 建模分包工作流.md
│                                      当前唯一执行SOP
├── 生境分析方案与工作流.md          正式生境分析计划
├── 组学分析方案.md                  图像预处理与整块肿瘤特征技术规范
├── AGENTS.md                        长任务运行、数据边界与环境约定
├── PROJECT_STATUS.md                当前执行位置与门禁状态
└── environment.yml                  Python 3.7影像组学环境定义
```

历史执行工作流《三十二、具体执行工作流：从 formal PASS 至 A-only model freeze》已移入`archive/protocol_history/`，仅用于追溯，不作为当前执行入口。

## 当前分析入口

1. `T2WI-HS-radiomics-analysis Primary v2 正式分析方案书与串行执行工作流.md`：当前 Primary v2 科学合同与分析主线。
2. `prognosis_analysis/primary/`：当前 A 验证、full-A 模型冻结和 B frozen-prediction 验证代码与登记。
3. `habitat_analysis/configs/main_cross_case_kmeans_k2_4mm.json`：三维SLIC 4 mm、跨病例K-means K=2主参数。
4. `habitat_analysis/cohort_definition.md`：宽松主分析集与严格敏感性集的冻结定义。
5. `habitat_analysis/input/README.md`：权威上游输入索引。
6. `habitat_analysis/analysis_freeze.md`：技术冻结参数及第一阶段锁状态。

当前主线已完成 A validation、full-A model freeze 和 B frozen external validation；下一阶段为 scientific interpretation / manuscript-ready analysis。Formal W08 v1 的中断与 R5/R6 记录仅保留在 `archive/formal_nested_cv_v1/`，不作为当前执行入口。

## 可复用上游资产

### feature_extract

- `result/`、`reader_2_result/`：R1/R2原始图像与ROI。
- `output/manifest.csv`、`output/scanner_map.csv`：图像—ROI追溯和设备映射。
- `output/preprocessed/`：肌肉均值归一化、`[1,1,2] mm`、无N4主预处理图像。
- `output/qc/`：主预处理日志、QC和归一化检查。
- `output/features_v2/`：H5次要探索模型可用的整块肿瘤候选。
- `output/roi_review_cases/`：已冻结的临床影像质量审阅材料。
- `scripts/`：主清单、DICOM扫描、预处理、核验、特征提取及技术敏感性所需脚本。

### prognosis_analysis

- `data/`：临床、影像、病理及预后原始表和数据字典。
- `output/modeling_v2/`：宽松主分析集A393/B107、严格敏感性集A137/B23及其分析模式。
- `output/qc/stage6_v2/`：H5整块肿瘤候选池审计。
- `scripts/build_model_dataset.py`：依据冻结筛选清单重建队列建模表。
- `scripts/stage6_qc.py`：整块肿瘤候选池QC。

## 双阶段冻结与输出层级

当前分析主线为：

```text
MRI/radiomics freeze
→ full-A habitat freeze
→ candidate freeze
→ Primary v2 fixed 5-fold A validation
→ full-A model freeze
→ frozen B external validation
→ final interpretation
```

Primary v2 的 A 验证使用固定 repeat-1 5-fold 和 training-only lambda 选择；full-A 模型冻结后，B 仅允许 frozen prediction。患者级结果目录全部保留在本地并由`.gitignore`排除。

## 运行环境

影像与ROI处理使用conda环境`t2_radiomics`：Python 3.7、PyRadiomics 3.0.1、SimpleITK 2.2.1。涉及SimpleITK时使用本机配置的ASCII junction调用项目文件；云端使用仓库相对路径。临床数据整理可使用本机Python 3.12；生存建模使用R 4.4.0。

预计运行超过40分钟的任务，须先小样本估时并遵守`AGENTS.md`中的监测节点规则。

## 归档

`archive/formal_nested_cv_v1/`保存 Formal W08 v1 的协议、R5/R6 审查与聚合证据；`archive/ft_validation_v1/`保存 FT 协议及 FT03/FT04/FT06 的安全聚合证据。`archive/protocol_history/`保存更早的任务书与审阅记录，其中“三十二”历史执行工作流及`n_init_equivalence_cancelled/`均不作为当前执行入口。`archive/exploration_20260828/`保存既往整块肿瘤影像组学与生境探索脚本、结果、配置和患者级材料，详细边界见其`MANIFEST.md`；该目录仅本地保留，不进入GitHub。所有归档均不得作为当前分析输入。
