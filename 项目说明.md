# 项目说明

本项目以直肠癌治疗前T2WI生境分析为当前主线。正式方法见《生境分析方案与工作流.md》，影像预处理技术细节见《组学分析方案.md》。

## 目录结构

```text
<project-root>\
├── feature_extract\                 原始影像、ROI及可复用的预处理/特征资产
├── habitat_analysis\                当前生境分析工作区
│   ├── configs\                     主参数和终点定义
│   ├── input\                       权威上游输入索引
│   ├── scripts\                     当前方案脚本
│   ├── output\                      分阶段结果目录
│   ├── README.md                    当前入口与执行边界
│   └── analysis_freeze.md           冻结参数及当前停止点
├── prognosis_analysis\              临床预后数据、当前队列清单及H5候选输入
├── manuscript\                      稿件阶段工作区
├── archive\                         历史方案与探索阶段归档
├── 生境分析方案与工作流.md          当前正式分析计划
├── 组学分析方案.md                  图像预处理与整块肿瘤特征技术规范
├── AGENT.md                         长任务运行与环境约定
└── environment.yml                  Python 3.7影像组学环境定义
```

## 当前分析入口

1. `habitat_analysis/configs/main_cross_case_kmeans_k2_4mm.json`：三维SLIC 4 mm、跨病例K-means K=2主参数。
2. `habitat_analysis/cohort_definition.md`：宽松主分析集与严格敏感性集的冻结定义。
3. `habitat_analysis/input/README.md`：主清单、预处理图像、筛选清单、临床数据及H5候选的权威路径。
4. `habitat_analysis/output/feasibility_A/`：下一阶段唯一允许写入的主结果目录，用于A=393全R1无结局技术干跑。
5. `habitat_analysis/analysis_freeze.md`：技术失败率门槛、病例排除规则、数据隔离和B集锁定状态。

在A集联合技术失败率确认＜5%并冻结成功/排除病例清单前，`habitat_maps_A/`之后的下游目录保持为空。

## 可复用上游资产

### feature_extract

- `result/`、`reader_2_result/`：R1/R2原始图像与ROI。
- `output/manifest.csv`、`output/scanner_map.csv`：图像—ROI追溯和设备映射。
- `output/preprocessed/`：肌肉均值归一化、`[1,1,2] mm`、无N4主预处理图像。
- `output/qc/`：主预处理日志、QC和归一化检查。
- `output/features_v2/`：H5次要探索模型可用的整块肿瘤候选。
- `output/roi_review_cases/`：已冻结的临床影像质量审阅材料。
- `scripts/`：主清单、DICOM扫描、预处理、核验、特征提取及技术敏感性所需脚本。

### prognosis_analysis

- `data/`：临床、影像、病理及预后原始表和数据字典。
- `output/modeling_v2/`：宽松主分析集A393/B107、严格敏感性集A137/B23及其分析模式。
- `output/qc/stage6_v2/`：H5整块肿瘤候选池审计。
- `scripts/build_model_dataset.py`：依据冻结筛选清单重建队列建模表。
- `scripts/stage6_qc.py`：整块肿瘤候选池QC。

## 生境输出阶段

`habitat_analysis/output/`依次包含：

```text
feasibility_A
→ habitat_maps_A
→ habitat_features_A
→ modeling
→ quick_check
→ nested_cv
→ final_model
→ validation_B
```

`reader_repeatability/`和`sensitivity/`用于后续技术描述与预设敏感性分析；`high_signal_eligibility_audit/`为冻结的队列筛选审计。

## 运行环境

影像与ROI处理使用conda环境`t2_radiomics`：Python 3.7、PyRadiomics 3.0.1、SimpleITK 2.2.1。涉及SimpleITK时使用本机配置的ASCII junction调用项目文件；云端使用仓库相对路径。临床数据整理可使用本机Python 3.12；生存建模使用R 4.4.0。

预计运行超过40分钟的任务，须先小样本估时并遵守`AGENT.md`中的监测节点规则。

## 归档

`archive/exploration_20260828/`保存既往整块肿瘤影像组学与生境探索脚本、结果、配置和文档，详细边界见其`MANIFEST.md`。归档目录不作为当前分析输入。
