# 项目说明

本项目以直肠癌治疗前T2WI生境分析、A集内部验证及B集一次性设备域外部验证为当前主线。科学主协议见《T2WI-HS-radiomics-analysis 后续探索性预后分析与双阶段冻结任务书.md》；当前唯一执行SOP为《T2WI-HS-radiomics-analysis Pre-W08 整改、协议补丁与后续 A-only 建模分包工作流.md》。历史执行工作流位于`archive/protocol_history/`，正式生境方法见《生境分析方案与工作流.md》，影像预处理技术细节见《组学分析方案.md》。

## 目录结构

```text
<project-root>\
├── feature_extract\                 原始影像、ROI及可复用的预处理/特征资产
├── habitat_analysis\                当前生境分析工作区
│   ├── configs\                     主参数和终点定义
│   ├── input\                       权威上游输入索引
│   ├── scripts\                     当前方案脚本
│   ├── output\                      分阶段结果目录
│   ├── README.md                    当前入口与执行边界
│   └── analysis_freeze.md           第一阶段技术冻结参数及状态
├── prognosis_analysis\              A集结局建模、最终模型冻结和B集验证工作区
├── manuscript\                      方法学证据与稿件阶段工作区
├── archive\                         历史协议及本地探索材料归档
│   └── protocol_history\            已完成或被替代的任务书与审阅报告
├── T2WI-HS-radiomics-analysis 后续探索性预后分析与双阶段冻结任务书.md
│                                      科学主协议
├── T2WI-HS-radiomics-analysis Pre-W08 整改、协议补丁与后续 A-only 建模分包工作流.md
│                                      当前唯一执行SOP
├── 生境分析方案与工作流.md          正式生境分析计划
├── 组学分析方案.md                  图像预处理与整块肿瘤特征技术规范
├── AGENTS.md                        长任务运行、数据边界与环境约定
├── PROJECT_STATUS.md                当前执行位置与门禁状态
└── environment.yml                  Python 3.7影像组学环境定义
```

历史执行工作流《三十二、具体执行工作流：从 formal PASS 至 A-only model freeze》已移入`archive/protocol_history/`，仅用于追溯，不作为当前执行入口。

## 当前分析入口

1. `T2WI-HS-radiomics-analysis Pre-W08 整改、协议补丁与后续 A-only 建模分包工作流.md`：当前执行SOP，规定从P4到G3及后续W08–W13的串行门禁。
2. `habitat_analysis/configs/main_cross_case_kmeans_k2_4mm.json`：三维SLIC 4 mm、跨病例K-means K=2主参数。
3. `habitat_analysis/cohort_definition.md`：宽松主分析集与严格敏感性集的冻结定义。
4. `habitat_analysis/input/README.md`：主清单、预处理图像、筛选清单、临床数据及H5候选的权威路径。
5. `habitat_analysis/analysis_freeze.md`：技术失败率门槛、技术冻结规则及第一阶段锁状态。
6. `prognosis_analysis/`：P4完整性复核、P5技术预检及第一阶段锁后的A-only结局分析工作区；第二把锁后才允许创建B验证结果。

当前状态为G2R PASS、P4待启动；P4通过后需补齐P5技术预检入口，经G2R2后完成50-fold technical-only preflight并通过G3，才允许正式W08。最终A模型冻结前不得读取B集。

## 可复用上游资产

### feature_extract

- `result/`、`reader_2_result/`：R1/R2原始图像与ROI。
- `output/manifest.csv`、`output/scanner_map.csv`：图像—ROI追溯和设备映射。
- `output/preprocessed/`：肌肉均值归一化、`[1,1,2] mm`、无N4主预处理图像。
- `output/qc/`：主预处理日志、QC和归一化检查。
- `output/features_v2/`：H5次要探索模型可用的整块肿瘤候选。
- `output/roi_review_cases/`：已冻结的临床影像质量审阅材料。
- `scripts/`：主清单、DICOM扫描、预处理、核验、特征提取及技术敏感性所需脚本。

### prognosis_analysis

- `data/`：临床、影像、病理及预后原始表和数据字典。
- `output/modeling_v2/`：宽松主分析集A393/B107、严格敏感性集A137/B23及其分析模式。
- `output/qc/stage6_v2/`：H5整块肿瘤候选池审计。
- `scripts/build_model_dataset.py`：依据冻结筛选清单重建队列建模表。
- `scripts/stage6_qc.py`：整块肿瘤候选池QC。

## 双阶段冻结与输出层级

当前执行门禁为：

```text
P4 integrity audit
→ P5 implementation
→ G2R2
→ P5 50-fold technical-only preflight
→ G3 PASS
→ formal W08
```

G3通过后，第二阶段位于`prognosis_analysis/`：

```text
A_endpoint_qc
→ A_modeling_raw
→ A_nested_validation
→ A_strict_sensitivity / A_volume_sensitivity
→ final_model
→ model_freeze_lock.json
→ B_external_validation
```

第一把锁只解锁A结局，第二把锁才解锁B一次性验证。患者级结果目录全部保留在本地并由`.gitignore`排除。

## 运行环境

影像与ROI处理使用conda环境`t2_radiomics`：Python 3.7、PyRadiomics 3.0.1、SimpleITK 2.2.1。涉及SimpleITK时使用本机配置的ASCII junction调用项目文件；云端使用仓库相对路径。临床数据整理可使用本机Python 3.12；生存建模使用R 4.4.0。

预计运行超过40分钟的任务，须先小样本估时并遵守`AGENTS.md`中的监测节点规则。

## 归档

`archive/protocol_history/`保存已经完成或被当前协议替代的任务书和审阅报告，其中“三十二”历史执行工作流由当前Pre-W08 SOP取代，不作为当前执行入口。`archive/exploration_20260828/`保存既往整块肿瘤影像组学与生境探索脚本、结果、配置和患者级材料，详细边界见其`MANIFEST.md`；该目录仅本地保留，不进入GitHub。所有归档均不得作为当前分析输入。
