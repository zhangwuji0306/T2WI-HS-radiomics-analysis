# 项目说明

本项目以直肠癌治疗前 T2WI 生境分析和 Primary Prognostic Analysis v2 为当前正式方法。Primary v2 已完成固定 full-A habitat、repeat-1 五折 A 验证、full-A 模型冻结和 frozen B 外部验证；下一阶段依次开展示例分析、次要结局分析和敏感性分析。

## 当前活动树

```text
<project-root>\
├── feature_extract\
│   ├── configs\                     PyRadiomics 与预处理参数
│   └── scripts\                     清单、预处理、归一化、QC、特征提取
├── habitat_analysis\
│   ├── configs\                     当前主参数与终点定义
│   ├── scripts\                     当前生境生成、读取和队列脚本
│   ├── freeze_lock.json              生境冻结锁
│   ├── freeze_integrity_addendum.json
│   ├── feature_dictionary.md         当前特征定义
│   ├── cohort_definition.md          当前队列定义
│   └── output\                      本地 fixed full-A maps/features/cache
├── prognosis_analysis\
│   ├── primary\                     Primary v2 当前合同与运行代码
│   ├── configs\                     当前运行时配置与 schema
│   ├── scripts\                     当前仍复用的建模、QC 和 habitat radiomics 依赖
│   └── execution_status.json         当前状态
├── archive\
│   ├── formal_nested_cv_v1\          Formal W08 v1 历史方法与审查
│   ├── ft_validation_v1\             FT 历史开发与验证
│   ├── protocol_history\             Pre-W08 与方法换轨记录
│   └── project_status_history\       过期状态文件
├── README.md
├── PROJECT_STATUS.md
├── AGENTS.md
├── environment.yml
├── requirements-cloud.txt
├── setup.sh
└── tools\
```

## 当前入口

1. `prognosis_analysis/primary/README.md`：Primary v2 方法说明。
2. `prognosis_analysis/primary/protocol.json`：Primary v2 机器可读合同。
3. `habitat_analysis/README.md`、`habitat_analysis/configs/`、`habitat_analysis/freeze_lock.json`：当前 fixed full-A habitat 定义与冻结资产。
4. `feature_extract/configs/`、`feature_extract/scripts/`：当前预处理和特征提取公共逻辑。
5. `PROJECT_STATUS.md`、`prognosis_analysis/execution_status.json`：当前状态和下一阶段。

## 冻结定义

- 临床变量块 `C`、global habitat 块 `G`、`R_low=49`、`R_high=10`、`W_Original=107`。
- 模型集合为 `M0–M5`；A 验证使用固定 repeat-1 5-fold identity。
- fixed full-A habitat 的参数、structural-state 规则和 technical eligibility 由 `habitat_analysis/` 下的 lock/config/dictionary/README 共同定义。
- 后续分析复用上述冻结影像表型、特征提取逻辑、队列身份和时间点定义，不从历史 FT 或 Formal W08 目录读取运行依赖。

## 数据边界

原始影像、ROI、Slicer 工程、临床/病理/预后表、患者级特征和分析输出仅保留在本地受控环境。含真实影像号的映射和清单不得进入仓库；`archive/` 中的本地历史输出也不作为当前分析输入。

## 运行环境

影像与 ROI 处理使用 conda 环境 `t2_radiomics`，锁定版本见 `environment.yml`。本地长任务估时、单次进度检查和结果核验遵循 `AGENTS.md`。
