# AGENTS.md — 项目智能体工作约定

本文件为在本项目（直肠癌T2WI生境分析，方法见`生境分析方案与工作流.md`）内工作的智能体约定。

## 一、运行与监测约定

1. **预估时间**：启动任何预计耗时超过 **40 分钟**的脚本（预处理、特征提取、质控、试点、建模等）前，必须先基于小样本实测或历史记录给出预估时间，并等待使用者确认是否开始。
2. **启动后单次检查**：程序启动后，在「预估时间 ÷ 10」的时刻进行**一次**进度检查：确认进程存活、产物开始落盘、无异常报错；检查后至预估时间结束前**不再监测**，不轮询、不重复读取任务输出。
3. **预估时间到达**：预计时间内不再重复监测。若程序超出预计时间超过`min(预计时间/10, 10分钟)`，再检查一次并报告实际进度、异常和修正预估。
4. **结果核验**：长任务结束后，以产物文件（特征表/指标表/报告等）为准核对完整性，不以输出缓冲为唯一依据；发现异常立即报告，不静默重试或绕过。

## 二、仓库与数据边界

- GitHub/Codex仓库仅保存代码、配置、方法文档和不含原始影像号的项目状态信息。
- 原始影像、ROI、Slicer工程、临床/病理/预后表、患者级特征和分析输出均为本地材料，不得提交。
- 任何包含影像号的清单、表格、日志或报告，在进入仓库前必须使用匿名号；原始影像号—匿名号映射只保存在本地 `local_private/image_id_mapping.csv`。
- 不要把映射表、原始数据或患者级派生结果复制到仓库，也不要在云端任务中读取被 `.gitignore` 排除的本地数据。
- 若任务必须访问真实患者数据或原始影像，应在本地受控环境执行，不在Codex云端执行。

## 三、会话结束与GitHub同步

1. **默认同步要求**：每次对话任务完成后，将本次新增或修改、且符合本文件数据边界的内容提交并推送至项目仓库：<https://github.com/zhangwuji0306/T2WI-HS-radiomics-analysis>。只提交本次任务范围内的文件，不夹带使用者尚未确认或与当前任务无关的工作区改动。
2. **审核报告同步**：每份代码审核、方法学审核、质量审核或执行审阅报告均应纳入上述同步范围。报告如含患者隐私，必须先完成脱敏并复核，再上传脱敏版本；不涉及患者隐私的内容保持原意直接上传，不作不必要的删改。
3. **脱敏要求**：上传前删除或替换能够直接或间接识别患者的信息，包括真实影像号、病历号、姓名、精确日期、原始文件路径、影像截图中的身份信息，以及可与本地映射表重新关联的患者级标识。需要保留个体对应关系时仅使用仓库允许的匿名号，并避免上传可逆映射关系。
4. **禁止上传内容不变**：原始影像、ROI、Slicer工程、临床/病理/预后原始表、原始影像号—匿名号映射、患者级特征表和其他被`.gitignore`排除的敏感材料，不因“会话结束同步”而获得上传许可。审核报告应整理为不含上述材料的可共享文本或聚合结果。
5. **提交前核验**：推送前检查暂存文件、差异和仓库状态，确认不含患者隐私、密钥、凭据、本机绝对路径或意外的大文件；必要时运行与改动相称的编译或测试。推送完成后核对本地提交与远程分支一致。
6. **同步失败处理**：若因网络、认证、权限、远程冲突或Git服务异常无法上传，不得声称已完成同步；应保留安全的本地改动或提交，并在对话结束时报告具体阻塞原因、当前提交哈希及仍需执行的同步步骤。

## 四、运行环境速查

- 计算环境：conda 环境 `t2_radiomics`（Python 3.7.12 + pyradiomics 3.0.1 + SimpleITK 2.2.1，严格版本锁定）。
- 本地 conda 不在 PATH 时，用本机安装路径对应的 `conda.exe run -n t2_radiomics --no-capture-output python <脚本>` 调用。
- 本地涉及 SimpleITK 的脚本经本机配置的 ASCII junction 调用；该路径仅适用于本地 Windows，不得写入云端命令或作为仓库内的通用路径。
- Codex 云端使用 Linux/Bash 环境；云端依赖由根目录 `setup.sh` 和 `requirements-cloud.txt` 提供，使用仓库相对路径。
- 当前入口见`项目说明.md`；参数与方法锁定见`组学分析方案.md`（预处理与整块肿瘤特征技术规范）、`生境分析方案与工作流.md`及`habitat_analysis/analysis_freeze.md`；当前分析输出仅写入`habitat_analysis/output/`对应阶段目录。
