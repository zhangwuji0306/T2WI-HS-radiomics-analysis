# Formal Nested-CV v1 历史归档

状态：`archived / superseded`

本目录保存 Formal W08 v1 的非患者级协议、执行审查、R5/R6 门禁记录及聚合证据，供方法与工程历史追溯。目录内容不再作为当前分析入口、参数选择依据或脚本输入。

归档内容包括：

- 旧 Formal W08 执行 SOP；
- `W08_*` nested-CV、preflight 和 L0–L6 审查/报告；
- `R5*` 与 `R6*` 审查、复核、失败分类、整改和聚合证据；
- `legacy_scripts/` 中的旧 runner、provenance、preflight、probe 和 legacy builder；
- `legacy_tests/` 中仅服务旧工作流的回归测试。

当前主线为 Primary Prognostic Analysis v2。Formal v1 已被其替代；当前方法说明和合同见 `prognosis_analysis/primary/README.md` 及 `prognosis_analysis/primary/protocol.json`。完整历史状态仍可由 immutable tag `archive/formal-nested-cv-v1-20260913` 恢复。

本目录只纳入版本化 Markdown/JSON 协议与聚合审查材料，不包含原始影像、ROI、临床/病理/预后原始表、患者级特征、预测、分析输出、映射表、凭据或本机绝对路径。
