# 直肠癌 T2WI 生境分析方案与工作流

## 一、研究目标与分析边界

本研究在治疗前 T2WI 上具有高信号成分的直肠癌患者中，使用跨病例 K-means 将肿瘤划分为相对低信号生境（`H-low`）和相对高信号生境（`H-high`），评价生境特征对无病生存期（DFS）的预测价值，以及其在预设临床影像学模型基础上的增量价值。

生境名称仅表示肌肉归一化 T2 信号的相对高低。未经病理空间对应验证，不将其直接解释为黏液区、实性区或其他组织学实体。

### 1.1 当前主分析集

病例按 R1 原始图像进行高信号表型筛选，每例同时满足：

1. 肿瘤内至少存在1个信号强度不低于瘤周脂肪 ROI 均值的体素；
2. 上述高信号体素占肿瘤总体素比例≥0.1%。

|队列|定义|病例数|DFS事件数|36个月内事件|60个月内事件|
|---|---|---:|---:|---:|---:|
|A（开发）|GE DISCOVERY MR750 3T|393|89|62|81|
|B（设备留出验证）|其他 MRI 设备|107|27|14|23|

- 表中病例数为高信号表型筛选后的技术评估目标集。最终生境主分析队列由其中生境生成成功的病例构成；允许按第4节规则剔除不足5%的技术失败病例，并完整报告筛选前后人数。
- A/B 均来自同一机构，B 表述为设备留出验证。
- 严格高信号筛选集 A=137、B=23，保留为队列定义敏感性分析，不替代宽松主分析集。
- B 不参与生境尺度、聚类中心、特征处理、模型参数或判定阈值的确定。
- 预测时点为治疗前 MRI 检查后、治疗启动前。

### 1.2 终点与临床变量

- 主终点：DFS。
- 主要评价时点：3年和5年。
- 其他指标：Harrell C-index、校准、Brier/IBS及探索性决策曲线。
- 临床模型固定使用年龄、`CEA_log`、`mrT_4级`、`mrN_3级`、MRF、mrEMVI、thickness、EID及活检病理非腺癌共9个变量。
- 性别、length和distance仅用于队列描述，不进入临床、生境或联合模型。

## 二、图像输入与预处理

### 2.1 主输入

- 建模读者：R1。
- 图像：治疗前连续强度 T2WI。
- 肿瘤 ROI：标签1。
- 归一化：原始分辨率腐蚀后肌肉 ROI 均值归一化，`I' = I / μ_muscle`。
- 重采样：`[1,1,2] mm`；图像使用三维 B-spline，掩膜使用最近邻插值。
- N4：不启用。
- 裁剪、几何核查、异常记录和图像—ROI追溯规则沿用《组学分析方案.md》及现有主预处理产物。

高信号病例筛选使用原始图像与瘤周脂肪参考；生境聚类使用肌肉归一化后的主预处理图像。二者用途不同，不混用参考强度。

### 2.2 数据隔离

- 无结局技术运行仅读取影像号、图像、ROI、预处理参数和设备信息，不读取 DFS、CSS、OS或临床变量。
- 内部验证的聚类中心仅由相应外层训练折拟合。
- 最终 B 集生境仅使用全 A 锁定的预处理、聚类中心、标签方向和特征规则生成。

## 三、主生境构建方法

### 3.1 三维 SLIC 空间单元

主方法固定使用三维 SLIC 超体素：

- 目标空间尺度：4 mm；
- `MaximumNumberOfIterations=5`；
- `SpatialProximityWeight=10.0`；
- 不启用初始化扰动；
- 启用连通性约束；
- 固定单工作线程，以保证可重复性。

SLIC 在裁剪后的整幅图像上运行，随后仅保留与肿瘤 ROI 相交的超体素。每个超体素以肿瘤 ROI 内的肌肉归一化 T2 平均信号作为聚类变量。

### 3.2 跨病例 K-means

主聚类方法固定为跨病例 K-means：

1. 汇总训练数据中各病例 R1 肿瘤内超体素平均信号；每例用于拟合的超体素数上限为2000，超过时按固定随机种子抽样；
2. 固定 `K=2`、k-means++、`n_init=100`、`max_iter=300`、`tol=1×10^-4`及随机种子12345；
3. 按两个聚类中心由低到高排序并固定为 `H-low`、`H-high`；
4. 每个病例的超体素按最近聚类中心分配标签；
5. 肿瘤 ROI 外设为背景，肿瘤 ROI 内不得存在未分配体素；
6. 相邻同标签超体素共同构成三维生境区域，并记录连通成分。

全 A 技术干跑使用全 A 拟合共享中心，以确认主分析集的生境可生成性。内部验证时必须在每个外层训练折重新拟合共享中心，并原样应用到该外层验证折，禁止使用全 A 中心生成 OOF 预测。最终模型完成后，使用全 A 重新拟合唯一一组中心并应用于 B。

## 四、技术可生成性门槛

### 4.1 唯一停止门槛

任一病例出现以下情况即定义为技术失败：

- 图像或 ROI 无法读取、几何不一致或 SLIC 执行失败；
- 肿瘤内有效超体素少于2个，或超体素均值不足2个不同取值，导致 K=2 无法拟合；
- K-means中心非有限、两中心相同或未收敛；
- `H-low` 或 `H-high` 在肿瘤 ROI 内体素数为0；
- 肿瘤 ROI 内存在未分配体素；
- 生境图与原图/ROI网格不一致或标签方向错误。

技术门槛按病例去重后的联合失败率判定。同一病例即使同时出现多类错误，分子只计1例：

```text
联合技术失败率
= 出现任一技术失败的唯一病例数 / 本批次全部目标病例数

联合技术失败率 < 5%：通过
联合技术失败率 ≥ 5%：停止该阶段
```

对A=393的全病例技术干跑，最多允许19例持续性技术失败；20例及以上不通过。对B=107，最多允许5例失败；6例及以上视为迁移技术不合格。其他批次按实际分母直接计算，始终使用严格小于5%的规则。

可修复的文件、几何或实现错误应先修复并按原参数重跑，只有修复后仍存在的失败才进入最终失败率。通过门槛时，所有失败病例均须写入排除清单并从该阶段主分析队列中剔除；不得将空生境或失败特征填0，不得作为普通缺失插补，也不得根据结局决定保留。H1–H5的主要比较必须使用同一技术成功病例集。

双读者 Dice、ICC、ARI和生境纹理可计算比例均作为后续技术描述，不作为当前停止门槛。

### 4.2 分阶段核验

1. 在 A=393 的全部 R1 病例上完成一次无结局全 A 技术干跑；
2. 逐例输出成功状态、两类体素数、体积分数、聚类中心、未分配体素数、失败类型和失败原因；
3. 先处理可修复错误，再按唯一病例计算联合技术失败率；
4. 若失败率＜5%，冻结成功病例清单和技术排除清单，后续A集主分析仅使用成功病例；冻结后再读取结局并重新报告总DFS事件数及3年/5年事件数；若失败率≥5%，暂停流程并提交失败清单；
5. 每个外层训练折及验证折生成生境时重复相同判定；低于5%时剔除失败病例，并保证同一折内H1–H5使用相同评价病例；
6. 最终全A中心应用于B后，独立计算B的联合技术失败率。低于5%时剔除失败病例并继续验证，达到或超过5%时报告方法迁移技术不合格。

技术排除不改变原始宽松/严格高信号队列定义。所有报告必须同时给出目标病例数、技术成功病例数、排除病例数、联合失败率及各失败类型计数。不得改用病例内K-means或根据结局选择替代方法。

### 4.3 当前执行状态

现有18例同序列R1/R2技术数据按跨病例K-means、4 mm重新核对后，R1有7例空生境，R2有3例空生境，任一读者出现空生境共8例；未分配体素为0。按新门槛，R1失败率为38.9%，双读者任一侧失败率为44.4%，均高于5%。该18例为既往双读者技术子集，不替代A=393全R1的正式技术干跑。

跨病例K-means、4 mm仍为当前主方法候选。全A特征生成和预后建模停留在技术核验阶段；下一步应在A=393完成无结局全病例干跑，修复可修复错误后计算联合失败率。失败率＜5%时剔除失败病例并进入第5节，失败率≥5%时停止并提交失败清单。

## 五、生境特征体系

### 5.1 低维描述符

每例至少生成以下预设描述符：

1. `H_high_fraction`：`H-high` 体积占肿瘤总体积的比例；
2. `H_high_largest_component_fraction`：`H-high` 最大连通成分占其总体积的比例；
3. `H_high_component_density`：`H-high` 连通成分数除以肿瘤体积；
4. `habitat_interface_density`：高、低生境交界面积除以肿瘤体积；
5. `H_high_radial_index`：`H-high` 体素到肿瘤边界的归一化距离均值；
6. `signal_contrast`：`H-high` 与 `H-low` 肌肉归一化 T2 中位信号差；
7. 两类生境的体素数、绝对体积、平均/中位信号及聚类中心间距，作为描述和质控变量。

### 5.2 生境内影像特征

- 主候选包括两个生境的 Original 一阶特征和纹理特征。
- PyRadiomics 内部重采样和归一化关闭；固定箱宽沿用 `binWidth=0.248808`。
- 标准 PyRadiomics Shape不进入生境高维候选，空间形态由预设低维描述符表示。
- Wavelet和LoG不进入主生境模型，可在主结果冻结后作探索性分析。
- 生境纹理可计算比例只作描述，不决定是否继续生境分析。
- 对非空生境尝试提取全部预设特征；任一候选特征若在 A-R1 中出现非有限值或提取失败，则从该特征的主候选池整体删除并记录，不对结构性缺失填0，也不按病例选择性保留。
- 双读者特征 ICC 在后续技术评价中报告，不作为主方法、尺度或候选特征的停止门槛。

## 六、建模数据与模型体系

### 6.1 建模数据

- 宽松主分析集为主要建模队列；A/B归属保持不变。
- 高信号筛选后的A393/B107为技术评估目标集；最终建模样本量和事件数以剔除不足5%的技术失败病例后的成功病例集为准，并在流程图和基线表中同时报告筛选前后人数。
- 严格高信号集使用完全相同的冻结流程作队列定义敏感性分析。
- 影像组学候选保持原始尺度；插补、近零方差、相关去重和标准化全部在外层训练折内拟合。
- 临床缺失值沿用 `mice` 折内多重插补：`m=20`、`maxit=20`。
- 生境生成失败不作为普通缺失进行插补。联合失败率＜5%时记录并剔除失败病例，达到或超过5%时触发第4节的技术停止规则。

### 6.2 模型体系

|模型|预测变量|角色|
|---|---|---|
|H1|9个预设临床影像学变量|临床基准|
|H2|预设低维生境描述符|生境描述符独立价值|
|H3|低维描述符＋可用的生境内一阶/纹理候选|生境影像特征模型|
|H4|9个临床变量强制纳入＋生境特征惩罚|主要联合模型|
|H5|H4＋整块肿瘤影像组学候选|次要探索模型|

- H1使用无变量选择 Cox 模型。
- H2在外层训练折内进行近零方差与`|r|>0.90`相关去重后拟合 Cox模型；相关冲突按第5.1节编号优先。
- H3使用 LASSO-Cox，`alpha=1`、`lambda.1se`。
- H4/H5使用单阶段`glmnet`；9个临床变量`penalty.factor=0`，其余影像变量接受L1惩罚。
- `lambda.min`和Elastic Net `alpha=0.9`仅作预设敏感性分析。

## 七、A集快速检查与正式内部验证

### 7.1 一次外层5折快速检查

正式重复验证前，先在A集运行一次外层5折：

```text
A技术成功队列（≤393）
└─ 外层5折（按DFS事件分层）
   ├─ 外层训练折
   │  ├─ 生成4 mm SLIC超体素
   │  ├─ 拟合跨病例K=2共享中心
   │  ├─ 计算训练折联合失败率并剔除失败病例
   │  ├─ 生成生境描述符和可用生境特征
   │  ├─ 折内插补、特征处理和内层调参
   │  └─ 拟合H1–H4，H5仅在资源允许时运行
   └─ 外层验证折
      ├─ 使用该训练折共享中心生成生境
      ├─ 计算验证折联合失败率并剔除失败病例
      ├─ 使用训练折锁定的插补和特征处理参数
      └─ 生成3年、5年OOF预测
```

快速检查报告：

- 每折聚类中心、中心间距和两类生境比例分布；
- 每折及全A的目标病例数、技术成功数、联合失败率、排除病例及分类失败计数；
- H1–H4的3年和5年OOF AUC及Harrell C-index；
- H4−H1在3年和5年的方向与点估计；
- 校准方向、非零特征数、预测完整性和运行警告。

### 7.2 快速检查分支

|结果|处理|
|---|---|
|任一目标批次联合技术失败率≥5%|停止该阶段并提交失败清单，不进入B|
|联合技术失败率＜5%|记录并剔除失败病例，在同一成功病例集上比较H1–H4|
|H2/H3在3年和5年均接近随机，且H4不优于H1|停止生境预后路线，不增加特征族或更换聚类方法|
|H4仅一个时点改善，或改善幅度小但方向可解释|进入正式A集重复内部验证，重点评价不确定性与稳定性|
|H4两个时点方向一致、校准不恶化|进入正式A集重复内部验证|

快速检查不根据结果修改K值、4 mm尺度、跨病例聚类方式、标签方向或主临床变量。

### 7.3 正式内部验证

- 外层5折×50次重复，按DFS事件分层；内层5折选择`lambda.1se`。
- 每次外层拆分均重新拟合跨病例聚类中心及全部训练依赖参数。
- 每折保存技术成功与排除清单；失败率＜5%时仅对成功病例生成预测，所有模型使用同一配对评价集，并报告OOF预测覆盖率。
- 报告3年/5年时间依赖AUC、C-index、校准、Brier/IBS和H4−H1配对差值。
- 重复分布用于稳定性描述；正式95%置信区间采用患者层面的配对bootstrap。
- 报告聚类中心分布、特征入选频率、非零特征数及全部失败日志。

## 八、全A最终拟合与B集验证

### 8.1 全A冻结

内部验证完成且满足进入B的条件后：

1. 使用全A的4 mm SLIC超体素拟合最终K=2共享中心；
2. 核验全A联合技术失败率＜5%，冻结技术成功病例和排除病例清单；
3. 从最终拟合队列剔除失败病例，在其余病例上锁定生境特征清单、插补参数、标准化参数、λ、系数和基线累积风险；
4. 保存只读参数包、运行清单、失败率和排除原因。

### 8.2 一次性B验证

- B仅应用全A锁定的共享中心，不重新聚类、不重新排序标签。
- 首先完成B生境可生成性核验，再生成预测。
- 若B联合技术失败率＜5%，记录并剔除失败病例，在其余B病例上报告H1–H4的3年/5年AUC、C-index、校准、Brier/IBS、探索性DCA和H4−H1配对差值。
- 若B联合技术失败率≥5%，报告方法迁移技术不合格；不在B重拟合中心、不切换病例内K-means，也不据B结果修改方法。
- OPTIMA和MULTIVA分别作设备分层描述；小样本设备只作病例级或合并描述。

## 九、后续技术评价与敏感性分析

### 9.1 双读者评价

双读者评价在主方法和4 mm尺度锁定后进行，不用于重新选择方法。对同序列R1/R2病例报告：

- ROI Dice；
- 两类生境 Dice和adjusted Rand index；
- H-high体积分数及低维描述符ICC(2,1)；
- 生境特征ICC和可计算情况；
- 聚类中心、空生境、未分配体素及叠加图人工复核结果。

双读者结果用于说明测量误差和研究限制，不作为当前生境流程的停止条件。

### 9.2 技术敏感性

以下分析只评价稳健性，不替代跨病例K=2、4 mm主方法：

1. 病例内K=2；
2. 跨病例K=3；
3. SLIC 6 mm和8 mm；
4. `[2,2,2] mm`重采样；
5. N4校正；
6. ROI面内1体素侵蚀与膨胀。

### 9.3 模型与队列敏感性

1. 严格高信号筛选集；
2. 临床变量完整病例；
3. `lambda.min`；
4. Elastic Net `alpha=0.9`；
5. mrT/mrN哑变量编码；
6. 所有预测变量统一惩罚；
7. OS、CSS及病理对应探索。

敏感性分析不得根据B性能重新指定为主分析。

## 十、输出与审计

```text
habitat_analysis/
├── configs/                         # 冻结的4 mm跨病例K=2参数
├── scripts/                         # 生境生成、QC、特征与建模脚本
├── output/
│   ├── feasibility_A/               # A=393全病例技术失败率与排除清单
│   ├── habitat_maps_A/              # A集技术成功病例R1生境标签图
│   ├── habitat_features_A/          # A集技术成功病例描述符及生境特征
│   ├── reader_repeatability/         # 后续双读者评价
│   ├── modeling/                     # 生境建模表
│   ├── quick_check/                  # A集一次5折快速检查
│   ├── nested_cv/                    # A集50×5正式内部验证
│   ├── final_model/                  # 全A锁定中心和模型包
│   ├── validation_B/                 # 一次性B集验证
│   └── sensitivity/                  # 技术、队列和模型敏感性
└── analysis_freeze.md                # 参数冻结、偏离和首次查看B记录
```

每次运行必须保存：输入病例清单、配置文件、随机种子、软件版本、聚类中心、逐例两类体素数、未分配体素数、失败类型与原因、联合失败率、技术成功清单、排除清单、运行时间和结局/B集读取边界。

预计超过40分钟的任务必须先基于小样本试运行估算时间。运行期间只在预计时间的十分之一节点检查一次；预计时间内不重复轮询，超出预计时间超过`min(预计时间/10, 10分钟)`后再检查并报告。

## 十一、执行工作流

```text
锁定宽松主分析集A=393/B=107
→ 锁定肌肉归一化、[1,1,2] mm、无N4
→ 锁定三维SLIC 4 mm＋跨病例K=2主方法候选
→ 在A=393完成无结局全病例技术干跑
→ 修复文件、几何或实现错误，并按唯一病例计算联合技术失败率
   ├─ 失败率≥5%：停止并提交失败清单
   └─ 失败率＜5%：冻结技术成功清单，记录并剔除失败病例
→ 对技术成功的A集R1病例生成生境图、低维描述符及可用生境特征
→ 构建A集H1–H4建模数据
→ 完成一次外层5折快速检查，报告3年和5年OOF AUC
   ├─ 任一目标批次失败率≥5%：停止，不进入B
   ├─ 失败率＜5%：剔除失败病例并在配对成功病例集上评价
   ├─ 无预测信号：停止生境预后路线
   └─ 存在一致改善或不确定灰区：进入50×5正式内部验证
→ 完成A集50×5重复嵌套CV及不确定性评价
→ 完成后续双读者重复性描述和预设敏感性分析
→ 使用全A拟合并冻结最终共享中心、特征与模型
→ 一次性应用于B并计算联合技术失败率
   ├─ B失败率≥5%：报告迁移技术不合格，不调整方法
   └─ B失败率＜5%：剔除失败病例并完成3年/5年外部性能评价
→ 完成严格筛选集敏感性、病理对应和设备分层描述
→ 冻结表格、图形、日志和统计报告
→ 进入稿件写作
```

## 十二、Analysis Freeze清单

1. 宽松高信号主分析集A=393、B=107；严格集为敏感性分析；
2. 主终点DFS，3年和5年为主要评价时点；
3. 9个预设临床变量；性别、length、distance不进入模型；
4. 肌肉均值归一化、`[1,1,2] mm`、无N4；
5. 三维SLIC空间尺度固定为4 mm；
6. 主方法为跨病例K-means，K=2；
7. k-means++、`n_init=100`、`max_iter=300`、`tol=1×10^-4`、随机种子12345；
8. 标签按聚类中心由低到高固定为`H-low/H-high`；
9. 聚类中心仅由对应训练数据拟合；
10. 唯一技术停止门槛为目标病例中空生境、算法失败、未分配体素或几何/标签错误的去重联合病例比例≥5%；低于5%时记录并剔除失败病例后继续；
11. 双读者重复性和生境纹理可计算比例作为后续描述，不作为停止门槛；
12. 低维描述符定义及Original生境特征范围；
13. 折内插补、特征处理、标准化、聚类和模型调参；
14. H1–H5模型层级，H4为主要联合模型；
15. A集一次5折快速检查及5折×50次正式内部验证；
16. B一次性设备留出验证，不参与方法选择；
17. 病例内K=2、K=3、6/8 mm、2 mm重采样、N4和ROI扰动仅为敏感性分析；
18. 所有技术失败和偏离均保留逐病例审计记录。
