# W08 local L1 parameter and observability audit

## Result

L1 is complete for the current source tree. The W08 formal provider, nested-CV frame provider, and technical preflight use the same immutable `KMEANS_PARAMETERS` object from `prognosis_analysis/scripts/w08_kmeans_parameters.py`.

The shared source is the repository-located configuration:

`habitat_analysis/configs/main_cross_case_kmeans_k2_4mm.json`

The complete validated contract is:

```text
algorithm=kmeans
k=2
initialization=k-means++
n_init=100
max_iter=300
tol=1e-4
```

Missing keys, type errors, and any value mismatch fail closed. The production source scan found no active `n_init=10` use.

## Progress observability

`progress.json` has a closed aggregate-only schema. It records only the W08 stage, status, repeat/fold/run name, completed and total fold/run counts, timestamps, elapsed time, and the four B-access flags. Unknown fields, patient fields, performance fields, and other free keys are rejected. The four B-access flags are forced to `false`.

Progress is written through the existing atomic JSON replacement path. Missing callbacks preserve the in-memory execution path. Callback exceptions and progress-write exceptions do not alter model calculation; progress-write failure counts are retained in the formal audit or run state.

## Verification

- Targeted L1 tests: 6/6 passed.
- Existing W08 targeted tests: 87/87 passed.
- Post-adjustment transaction and release-gate regression: 52/52 passed.
- Python compilation passed for the four L1 production scripts and the L1 test.
- `git diff --check` passed.
- No formal W08 run, nested-CV production run, prediction output, performance result, model-freeze lock, or B input was accessed or generated by this work unit.
- Temporary test directories were removed by the test fixtures.
